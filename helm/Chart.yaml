apiVersion: v2
name: agent-studio
description: Helm chart for agent-studio multi-service application (FastAPI backend, Celery workers, Nginx SPA frontend, PostgreSQL, Redis)
type: application
version: 0.1.0
appVersion: "latest"
